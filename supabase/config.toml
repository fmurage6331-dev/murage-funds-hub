project_id = "cbfciiehhpkpdtxrltrc"

# Africa's Talking sends form-encoded callbacks without a Supabase JWT.
# whatsapp-bot requires AFRICASTALKING_WEBHOOK_SECRET in the callback URL instead.
[functions.whatsapp-bot]
verify_jwt = false
